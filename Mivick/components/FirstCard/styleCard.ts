import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  cardContainer: {
    backgroundColor: '#2D3748', // cor fixa local
    borderRadius: 8,
    padding: 16,
    marginVertical: 8,
  },
});
