import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    backgroundColor: '#FFFFFF', // fundo branco
    borderWidth: 4,
    borderColor: '#F85200', // borda laranja
    borderRadius: 8,
    padding: 12,
    color: '#1A202C', // texto preto/cinza escuro
    fontSize: 16,
  },
});
