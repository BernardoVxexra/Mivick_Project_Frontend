import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    backgroundColor: '#FFFFFF', // fundo branco
    borderWidth: 1,
    borderColor: '#CBD5E0', // cinza claro padrão
    borderRadius: 8,
    padding: 12,
    color: '#1A202C', // texto preto/cinza escuro
    fontSize: 16,
  },
});
