import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  btn: {
    backgroundColor: '#FF4500', // cor fixa aqui
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 16,
  },
  text: {
    fontSize: 20,
    color: '#FFFFFF',
    fontFamily: "SansBoldPro"
  },
});
